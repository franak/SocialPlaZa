function ptoLogin(userName, password){	    var p = ds.Usuarios.find("NombreAcceso = :1",userName);//     if (p == null) //if the user name does not exist in our datastore class        return false; //let Wakanda try to find it in the internal directory    else // the user name is known    {       if (p.Password == password)   {           //debugger;                      // debugger;            var theGroups = [];                        switch (p.accessType){				case 1:				theGroups = ["Internal"]; break;				case 2:				theGroups = ["Supervisor"]; break;				case 3:				theGroups = ["Usuarios"]; break;				case 4:				theGroups = ["Prueba"]; break;			}			       		       		//debugger;       		            var connectTime = new Date();                        return {                 ID: p.UserID,                 name: p.NombreAcceso,                 fullName: p.NombreAcceso,                belongsTo: theGroups,                storage:{                    time: connectTime                }            };       } else {            return { error: 1024, errorMessage:"invalid login" };    }    	}};